# Changelog – @venlyfinance/settlement-mcp

## 0.1.2 – unreleased

- `homepage` now https://docs.venlyfinance.com (npm page surfaced the repo URL prominently).
- Add `mcpName` (`io.github.timdierckxsens/venly-settlement-mcp`) to package.json: the ownership-verification marker the official MCP registry requires for npm packages. Metadata only, no functional change; enables the registry listing.

## 0.1.1 and earlier

Released alongside `@venlyfinance/sdk` 0.1.1 (2026-07-25): realignment to the live published OpenAPI specs. History: repository root `CHANGELOG.md` and git log.
